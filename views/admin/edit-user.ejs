<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .edit-user-container {
            margin-top: 50px;
            padding: 30px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        h1 {
            font-size: 2.5rem;
            font-weight: bold;
            color: #007bff;
            text-align: center;
            margin-bottom: 30px;
        }
        .form-control {
            border-radius: 5px;
        }
        .btn-update, .btn-delete {
            width: 100%;
            margin-top: 20px;
        }
        .go-dashboard {
            display: block;
            text-align: center;
            margin-top: 20px;
            font-size: 1.1rem;
        }
        .btn-delete {
            background-color: #dc3545;
            color: white;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="edit-user-container">
                    <h1>Edit User</h1>

                    <!-- Edit User Form -->
                    <form action="/admin/edit-user" method="post">
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" name="name" class="form-control" id="name" placeholder="Enter name" value="<%= user.name %>" required>
                        </div>

                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="email" name="email" class="form-control" id="email" placeholder="Enter email" value="<%= user.email %>" required>
                        </div>

                        <div class="form-group">
                            <label for="mno">Mobile</label>
                            <input type="text" name="mno" class="form-control" id="mno" placeholder="Enter Mobile" value="<%= user.mobile %>" required>
                        </div>

                        <input type="hidden" name="id" value="<%= user._id %>">

                        <!-- Edit Button -->
                        <button type="submit" class="btn btn-primary btn-update">Update User</button>
                    </form>

                    

                    <a href="/admin/dashboard" class="go-dashboard">Go to Dashboard</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

</body>
</html>
