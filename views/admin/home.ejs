<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Home</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- jQuery and Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    
    <style>
        body {
            background-color: #f8f9fa;
        }
        .admin-container {
            margin-top: 50px;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        .admin-image {
            border-radius: 50%;
            border: 3px solid #007bff;
        }
        .admin-table {
            width: 100%;
            margin-top: 30px;
            font-size: 1.1rem;
        }
        .admin-table td {
            padding: 15px;
            border-bottom: 1px solid #dee2e6;
        }
        h1 {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 20px;
            color: #007bff;
        }
        .btn-custom {
            width: 100%;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="admin-container text-center">
                    <h1>Welcome, <%= admin.name %>!</h1>
                    
                    <!-- Admin Image -->
                    <img src="/userImages/<%= admin.image %>" class="admin-image" width="150px" height="150px" alt="<%= admin.image %>">
                    
                    <!-- Dashboard and Logout Buttons -->
                    <div class="row mt-4">
                        <div class="col-md-6">
                            <a href="/admin/dashboard" class="btn btn-primary btn-custom">Go to Dashboard</a>
                        </div>
                        <div class="col-md-6">
                            <a href="/admin/logout" class="btn btn-danger btn-custom">Logout</a>
                        </div>
                    </div>
                    
                    <!-- Admin Information Table -->
                    <table class="admin-table table table-bordered mt-4">
                        <tbody>
                            <tr>
                                <td><strong>Email:</strong></td>
                                <td><%= admin.email %></td>
                            </tr>
                            <tr>
                                <td><strong>Mobile:</strong></td>
                                <td><%= admin.mobile %></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

    <!-- Prevent back button script -->
    <script>
        (function() {
            history.pushState(null, null, location.href);
            window.onpopstate = function () {
                history.go(1);
            };
        })();
    </script>
</body>
</html>